version: '2'

services:
  packet-forwarder:
    build: .
    environment:
      - 'DBUS_SYSTEM_BUS_ADDRESS=unix:path=/host/run/dbus/system_bus_socket'  
    privileged: true
    volumes:
      - 'pktfwdr:/var/pktfwd'
    labels:
      io.balena.features.dbus: '1'
    cap_add:
      - SYS_RAWIO
